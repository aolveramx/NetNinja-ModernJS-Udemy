// else if statements

const password = 'pass'

if (password.length >= 12) {
  console.log('That password is might strong');
} else if (password.length >= 8) {
  console.log('That password it long enough')
} else {
  console.log('That password it not long enough')
}