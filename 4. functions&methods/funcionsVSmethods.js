// remember a function is: block of code to run something that could be call at some point in our code
const name = 'shaun'

// functions
const greet = () => 'hello'

let resultOne = greet()
console.log(resultOne)

// methods
/*
calling by dot .
are functions which are associated to an object or data type like a string
difference: how we called them and where are defined
*/
let resultTwo = name.toUpperCase()
console.log(resultTwo)